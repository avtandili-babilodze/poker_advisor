# Keep Windows batch files as CRLF so they always run correctly on Windows,
# regardless of the OS they were committed from.
*.bat text eol=crlf
*.cmd text eol=crlf

# Shell scripts must stay LF or they break on Linux/macOS.
*.sh text eol=lf
